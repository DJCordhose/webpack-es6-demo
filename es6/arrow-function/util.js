//export const displayInPage = (text) => {
//    return document.body.innerHTML +=
//        `${text}<br>`;
//};

export const displayInPage = text =>
    document.body.innerHTML +=
        `${text}<br>`;
